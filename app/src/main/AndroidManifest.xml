<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.whitessmoke.melisearcher">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeliSearcher"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.views.DetailtView"></activity>
        <activity
            android:name=".ui.views.ResultsView"
            android:configChanges="orientation"
            android:label="@string/resultados" />
        <activity
            android:name=".ui.views.SearchView"
            android:label="@string/buscador"
            android:theme="@style/Theme.MeliSearcher.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>